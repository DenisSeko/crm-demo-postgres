[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA redirect za Vue Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# API proxy do Railway backenda
[[redirects]]
  from = "/api/*"
  to = "https://crm-staging-app.up.railway.app/api/:splat"
  status = 200
  force = true